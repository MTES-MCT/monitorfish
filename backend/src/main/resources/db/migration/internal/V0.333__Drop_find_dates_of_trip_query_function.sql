DROP FUNCTION
    find_dates_of_trip(
        VARCHAR,
        VARCHAR,
        TIMESTAMP WITHOUT TIME ZONE,
        TIMESTAMP WITHOUT TIME ZONE
    ),
    find_dates_of_trip(
        VARCHAR,
        VARCHAR
    );
