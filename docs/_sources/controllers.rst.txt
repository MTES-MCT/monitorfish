==========
Controlers
==========

The ``controllers`` flow extracts the list of control units from the FMC2 database and inserts 
the data into the ``controllers`` table of the Monitorfish database.

It is scheduled to run every day.