set -e

# Frontend: lint + format staged files only (oxlint --fix / prettier, re-staged by lint-staged).
# Kept intentionally minimal so commits stay fast (< 20s). Slower, whole-project checks live in
# the pre-push hook (tsc, type-aware lint), and backend ktlint is enforced in CI (with a baseline).
npx lint-staged --cwd ./frontend
