import type { Option } from '@mtes-mct/monitor-ui'

export const HAS_GLOBAL_FAO_AREA_AS_OPTIONS: Array<Option<boolean>> = [
  { label: 'Une seule zone de capture', value: true },
  { label: 'Différentes zones de capture', value: false }
]
