import { COLORS } from '../../constants/constants'
import styled from 'styled-components'

const EmptyCardTable = styled.div`
  text-align: center;
  color: ${COLORS.slateGray};
  margin-top: 20px;
`

export default EmptyCardTable
