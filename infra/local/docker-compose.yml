version: '3'
services:
  geoserver:
    image: kartoza/geoserver:2.18.0
    restart: unless-stopped
    ports:
      - 8081:8080
    volumes:
      - geoserver-data:/opt/geoserver/data_dir

volumes:
  geoserver-data:
    driver: local